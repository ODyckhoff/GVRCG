<IfModule mod_rewrite.c>
    RewriteEngine on
    RewriteRule (.*) pub/$1 [L]
 </IfModule>

AuthName "Restricted Garw Valley Railway Demo Site. Contact Owen For Access."
AuthType Basic
AuthUserFile /var/www/scones/dev/gvrcg/.htpasswd
AuthGroupFile /dev/null
require valid-user
